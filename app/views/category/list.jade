extends ../layout.jade

block title
  title Category list

block content
  h1
    | Category list

  if items.length
    .list-group
      each item in items
        a.list-group-item(href="/products?cid=#{item.id}") #{item.name}
  else
    | Category list empty
